#!/usr/bin/php
<?php
namespace FooBar;

//bootstrap
require_once('bootstrap.php');

use \PersonalityInsightsPHP;
use \PersonalityInsightsPHP\StdOut;
use \PersonalityInsightsPHP\RestAPI;
use \PersonalityInsightsPHP\Config;

$params = $argv;


//write stdout header
for ($x=0;$x<10;$x++) {
    Stdout::line("\n");
}


RestAPI::factory()
    ->connect(Config::getInstance(), function($res){

        $body = $res->getResult();
        var_dump($body);
    });
